enum AppButtonVariant { primary, outlined, dark, surface }

enum AppIconButtonVariant {
  primary, // Red background, white icon
  outlined, // Transparent background, dark border & dark icon (Matches Screenshot)
  dark, // Carbon black background, white icon
  surface, // Light grey container background, dark icon
}

enum AppButtonSize { small, medium, large }
